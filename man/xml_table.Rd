% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract.R
\name{xml_table}
\alias{xml_table}
\alias{xml_list}
\title{Extract tables and lists}
\usage{
xml_table(x, header = NA, trim = TRUE, ns = NULL, max_cells = 1e+07)

xml_list(x, trim = TRUE, ns = NULL)
}
\arguments{
\item{x}{A node or nodeset of \code{table} elements for \code{xml_table()}, or of
\code{ul} and \code{ol} elements for \code{xml_list()}. A document stands for its root.}

\item{header}{Use the first row as column names. \code{NA}, the default, does so
when every cell of the first row is a \code{th}.}

\item{trim}{Trim leading and trailing whitespace from each value.}

\item{ns}{Namespace URI of the table or list elements, \code{NA} for none,
\code{NULL} for any.}

\item{max_cells}{The most cells one table may expand to. A positive whole
number, or \code{Inf}.}
}
\value{
A list with one element per node in \code{x}: a data frame for
\code{xml_table()}, a list of items for \code{xml_list()}.
}
\description{
\code{xml_table()} turns \code{table} elements into data frames, and \code{xml_list()}
turns \code{ul} and \code{ol} elements into R lists. Both read well-formed XML, such
as XHTML or a table embedded in another XML format. They do not apply HTML
parsing rules: a \code{table} without \code{tbody} has exactly the rows it contains.
}
\details{
\strong{Tables.} The rows are the \code{tr} elements that are children of the
\code{table}, or of its \code{thead}, \code{tbody} or \code{tfoot}, in document order. A table
nested inside a cell is not descended into; extract it separately. The
cells are the \code{td} and \code{th} elements of a row, and a cell's value is its
text, as \code{\link[=xml_text]{xml_text()}} gives it. Every column is character: convert types
yourself, for example with \code{type.convert(df, as.is = TRUE)}.

\code{colspan} and \code{rowspan} are expanded by repeating the cell's value. A span
that is not a positive whole number counts as 1, and spans are capped at
HTML's own limits, 1000 columns and 65534 rows. A row span never adds rows
past the end of the table. Short rows are padded with \code{NA}.

Expansion can make a small document produce a very large table, so the
expanded size of each table is bounded by \code{max_cells}, and exceeding it is
a \code{zuxml_limit_error}.

\strong{Lists.} Each \code{li} child of a \code{ul} or \code{ol} becomes one item. An item is
its text as a string or, when the \code{li} has \code{ul} or \code{ol} children, a list
with two elements: \code{text}, the item's own text without the nested lists',
and \code{items}, the items of its nested lists, extracted the same way. Only
direct \code{ul} and \code{ol} children of an \code{li} count as nested lists. Comments
and processing instructions inside an item are ignored.
}
\examples{
doc <- xml_parse("<table>
  <tr><th>fruit</th><th>price</th></tr>
  <tr><td>apple</td><td>1.20</td></tr>
  <tr><td>pear</td><td>0.90</td></tr>
</table>")
xml_table(xml_root(doc))[[1]]

doc <- xml_parse("<ul>
  <li>fruit<ul><li>apple</li><li>pear</li></ul></li>
  <li>bread</li>
</ul>")
str(xml_list(xml_root(doc))[[1]])
}
\seealso{
\code{\link[=xml_find]{xml_find()}} to locate the tables or lists in a document, and
\link{zuxml-conditions} for the errors these raise.
}
