% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{zuxml-conditions}
\alias{zuxml-conditions}
\title{Conditions raised by zuxml}
\description{
Every error zuxml raises carries a condition class, so that it can be
caught by kind rather than by matching the message, which is not stable.
Every class below is a subclass of \code{zuxml_error}.
}
\details{
\describe{
\item{\code{zuxml_invalid_argument}}{An argument was not usable: an \code{x} that
is not a single string or a raw vector, an \code{encoding} other than UTF-8
for character input, a limit that is not a positive whole number, or a
flag that is not \code{TRUE} or \code{FALSE}.}
\item{\code{zuxml_parse_error}}{The input is not well-formed XML, ends too
early, or references an undefined entity.}
\item{\code{zuxml_encoding_error}}{The input is not valid in its encoding, or
names an encoding that neither Expat nor \code{\link[=iconv]{iconv()}} can read.}
\item{\code{zuxml_doctype_error}}{The document has a \code{DOCTYPE} declaration
and \code{allow_doctype} is \code{FALSE}, or it has an internal subset, which is
refused either way.}
\item{\code{zuxml_limit_error}}{A resource limit was reached. The subclasses
\code{zuxml_depth_limit}, \code{zuxml_node_limit}, \code{zuxml_attr_limit},
\code{zuxml_text_limit} and \code{zuxml_memory_limit} name which one.
\code{\link[=xml_serialize]{xml_serialize()}} raises the parent class alone when a node's text is
longer than an R string can hold.}
\item{\code{zuxml_memory_error}}{An allocation failed.}
\item{\code{zuxml_cancelled}}{A handler stopped the parse. It can only happen
through the C API, where a downstream package supplies the handler;
no exported R function raises it.}
}

A condition raised by a parse carries \code{line}, \code{column} and \code{byte_offset},
the position where the parser stopped; \code{status}, the C status's enumerator
name, such as \code{"ZUX_ERR_DEPTH_LIMIT"}; and \code{expat_code}, Expat's own error
code, which is \code{NA} when the failure was not Expat's. It is there for
diagnostics: do not branch on it. A limit error also carries \code{limit}, the
argument's name, such as \code{"max_depth"}, and \code{limit_value}, the value it was
given. A \code{zuxml_invalid_argument} condition carries \code{arg}, the name of the
argument at fault.
}
\examples{
tryCatch(
  xml_parse("<a><b></a>"),
  zuxml_parse_error = function(e) c(line = e$line, column = e$column)
)
tryCatch(
  xml_parse("<a><b/></a>", max_depth = 1),
  zuxml_limit_error = function(e) e$limit
)
}
