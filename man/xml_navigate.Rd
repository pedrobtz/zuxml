% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/node.R
\name{xml_navigate}
\alias{xml_navigate}
\alias{xml_root}
\alias{xml_parent}
\alias{xml_children}
\alias{xml_elements}
\alias{xml_find}
\title{Navigate an XML document}
\usage{
xml_root(x)

xml_parent(x)

xml_children(x)

xml_elements(x, name = NULL, ns = NULL)

xml_find(x, name = NULL, ns = NULL)
}
\arguments{
\item{x}{A \code{zuxml_document}, node, or nodeset.}

\item{name}{Local name to match, or \code{NULL} for any.}

\item{ns}{Namespace URI, \code{NA} for none, \code{NULL} for any.}
}
\value{
\code{xml_root()}, \code{xml_parent()}, \code{xml_children()}, \code{xml_elements()}
and \code{xml_find()} return a nodeset.
}
\description{
Every accessor is vectorized: given a nodeset of length \code{n} it returns a
result of length \code{n}, and the traversal functions return a single flat
nodeset.
}
\details{
Filtering is uniform. \code{name} matches the \emph{local} name. \code{ns = NULL} matches
any namespace, \code{ns = NA} matches only nodes in no namespace, and a string
matches that namespace URI. Matching never considers the prefix: two
prefixes bound to one URI are the same name.
}
\examples{
doc <- xml_parse("<r><a><b>1</b></a><b>2</b></r>")
xml_name(xml_children(xml_root(doc)))
xml_text(xml_find(doc, "b"))
}
