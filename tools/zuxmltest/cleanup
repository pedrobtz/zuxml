#!/bin/sh
# R CMD INSTALL runs this from the package directory, but a person may not.
# Run from zuxml's root, the relative paths below were zuxml's own, and
# deleted its src/Makevars.
cd "$(dirname "$0")" || exit 1
rm -f src/Makevars src/Makevars.win
rm -f src/*.o src/*.so src/*.dll
